var formpath = '../../components/kadonnut.jsx';
//jest.dontMock(formpath);
import kadonnutform from '../../components/spinner.jsx';
describe('some tests', _ => {
  it('changes the text after click', () => {
    expect(2).toBe(2);
  });

});